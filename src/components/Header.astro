---
import ThemeToggle from "./ThemeToggle.tsx";

const isEvolverPage = Astro.url.pathname.startsWith("/evolver");
const toggleHref = isEvolverPage ? "/" : "/evolver";
const toggleLabel = isEvolverPage ? "Solver" : "Evolver";
---

<header class="sticky top-0 z-40 border-b border-brand-100 bg-white/90 backdrop-blur dark:border-brand-700 dark:bg-brand-900/90">
  <div class="mx-auto flex max-w-6xl items-center justify-between px-4 py-3">
    <a href="/" class="flex items-center gap-2 font-extrabold text-brand-500 dark:text-white">
      <svg width="28" height="28" viewBox="0 0 28 28" fill="none" xmlns="http://www.w3.org/2000/svg" aria-hidden="true">
        <rect width="28" height="28" rx="7" class="fill-brand-500" />
        <g fill="none" stroke="#55C8FF" stroke-width="1.6">
          <path d="M8 8h4v4H8zM16 8h4v4h-4zM8 16h4v4H8zM16 16h4v4h-4z" stroke="#ffffff" stroke-opacity="0.35" />
          <path d="M10 10l8 8" />
        </g>
      </svg>
      <span>Word Hunt Solver</span>
    </a>

    <div class="flex items-center gap-3">
      <a href={toggleHref} class="inline-flex rounded-lg border border-brand-200 px-3 py-1.5 text-sm font-semibold text-brand-500 hover:bg-brand-50 dark:border-brand-700 dark:text-accent-400 dark:hover:bg-brand-800">
        {toggleLabel}
      </a>
      <ThemeToggle client:load />
    </div>
  </div>
</header>
